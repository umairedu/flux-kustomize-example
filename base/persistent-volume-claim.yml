apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data-pvcv1
  namespace: default
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: postgres-datav1
  storageClassName: standard